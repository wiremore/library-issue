import React, { useState, useEffect } from 'react';

export const Static = () => {
    return (
        <div style={{ padding: 16 }}>
            React Version: {React.version}
        </div>
    );
};
